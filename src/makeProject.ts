enum project_langs {
  HTML = 0,
  CSS,
  JS,
  TS,
  CPP,
  C,
  GDScript,
}

class project_template {
  constructor(name: string, image_path: string, description: string) {}
}

function make_project_template(proj_templ: project_template) {}
